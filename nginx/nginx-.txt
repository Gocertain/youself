ip -link -list
ip -s link list
env
getenforce

iptables -t nat -L
iptables -F

yum list|grep nginx

nginx -V/v   阿

systemctl restart nginx.server


rpm -ql nginx 				(nginx安装list)

curl -v http://...com/..>/dev/null	

curl http://../../..

head/tail -50 folder      header-50  end-50
tail -f folder(/etc/nginx/error.log,access.log,"/var/log/nginx/error.log,access.log"); 	查看错误日志		



ab -n "allnum" -c "onenum"   'http_url'   压力test
检查conf语法 
nginx -t -c /etc/nginx/nginx.conf         test

nginx -s reload -c /etc/nginx/nginx.conf  reload

ps -aux|grep nginx 				show PID